#!/bin/bash
sudo cp config/box-bridge.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable box-bridge
sudo systemctl start box-bridge
echo "Box bridge running on 0.0.0.0:9099"
